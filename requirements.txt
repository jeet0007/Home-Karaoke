blinker==1.9.0
click==8.4.2
Flask==3.1.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8
yt-dlp==2026.6.9
httpx>=0.27
python-dotenv>=1.0
ytmusicapi==1.7.4
flask-sock>=0.7.0
simple-websocket>=1.1.0
numpy>=1.26

# Optional: the isolated-vocal note guide (Demucs + Basic Pitch) lives in
# requirements-ml.txt - heavy (torch), opt-in. Without it there's no note
# guide (no low-quality fallback); lyrics, playback and scoring still work.
